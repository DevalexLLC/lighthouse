# lighthouse-agent dev configuration (compose dev overlay only).
# Site identity comes from the join token, not this file — all three dev
# agents share this config.
server:
  address: proxy:443
  sni: grpc.lighthouse.local   # routes to the gRPC listener at the proxy
state_dir: /var/lib/lighthouse-agent
log:
  level: debug
